
# RandomNumberGeneration

A starter OpenCL + C++ project for GPU-based random number generation.

## Features
- OpenCL platform/device discovery
- Kernel compilation
- GPU-side pseudo-random number generation
- CMake build system

## Build
```bash
mkdir build
cd build
cmake ..
cmake --build .
```
