
# Raytracing

A starter OpenCL + C++ GPU raytracing project.

## Features
- OpenCL GPU setup
- Simple ray generation kernel
- CPU-side image output
- PPM image export

## Build
```bash
mkdir build
cd build
cmake ..
cmake --build .
```
